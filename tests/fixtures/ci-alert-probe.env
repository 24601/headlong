# CI alert probe, 2026-08-22. NOT a real key: it is the hex digits counted
# backwards, twice. It exists only to turn the gitleaks job red once and
# prove the alert-main-red Slack post fires. Delete this file and allowlist
# the pattern in .gitleaks.toml in the follow-up commit.
OPENROUTER_API_KEY=sk-or-v1-fedcba9876543210fedcba9876543210
