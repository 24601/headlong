# tui/

The Rust source for `shelly-tui`, the terminal client for chatting with
an agent. `install.sh` builds it with cargo and places the binary at
`bin/shelly-tui`, which is gitignored because it is a build product.
