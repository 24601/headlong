# tui/

The Rust source for `shelly-tui`, the terminal client for chatting with
an agent. `install.sh` builds it with cargo (when cargo is installed) and
puts the binary on PATH next to the other tools (`~/.local/bin/shelly-tui`
by default).
